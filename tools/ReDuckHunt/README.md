# ReDuckHunt

Vitest checks for scripts under `scripts/`.

```bash
npm install
npm test
```

From repo root: `npm test`.

Checks: `meta.json`, `script.js` syntax, `cases.json` shape, `checkExpect` units.
Report: `test-results/report.md` (also posted on the Actions job summary).
